# Deployer key for X Layer TESTNET (terigon, chainId 1952). Generate: node -e "const {ethers}=require('ethers');const w=ethers.Wallet.createRandom();console.log(w.privateKey)"
DEPLOYER_PRIVATE_KEY=
# Contract address after deploy (written automatically)
CONTRACT_ADDRESS=
# Network: testnet only
RPC_URL=https://testrpc.xlayer.tech/terigon
CHAIN_ID=1952
